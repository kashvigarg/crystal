import 'dart:ui';

import 'package:flutter/material.dart';

class SizeConfig {
  static Size deviceSize = window.physicalSize;
  static double height = deviceSize.height;
  static double width = deviceSize.width;
}
